{
  "name": "effectionx",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test './**/*.test.ts'"
  },
  "workspaces": [
    "bdd",
    "context-api",
    "fx",
    "jsonl-store",
    "process",
    "raf",
    "signals",
    "stream-helpers",
    "task-buffer",
    "test-adapter",
    "timebox",
    "tinyexec",
    "watch",
    "websocket",
    "worker"
  ],
  "devDependencies": {
    "expect": "^29"
  },
  "overrides": {
    "effection": "^4.0.0-0"
  },
  "pnpm": {
    "overrides": {
      "effection": "^4.0.0-0"
    }
  },
  "dependencies": {
    "effection": "^3"
  }
}
